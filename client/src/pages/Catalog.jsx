import { Container, Typography } from "@mui/material";

const Catalog = () => {
    return (
        <div style={{ height: '100vh' }}>
            <Container>
                <Typography>
                    Catalog
                </Typography>
            </Container>
        </div>
    );
};

export default Catalog;