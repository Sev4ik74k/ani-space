import { Container, Typography } from "@mui/material";

const Catalog = () => {
    return (
        <div>
            <Container>
                <Typography>
                    Catalog
                </Typography>
            </Container>
        </div>
    );
};

export default Catalog;